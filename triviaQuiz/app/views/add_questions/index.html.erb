<div class="container">
<p id="notice"><%= notice %></p>

<h1>Add Questions</h1>

<table>
  <thead>
    <tr>
      <th>Question</th>
      <th>Correctoption1</th>
      <th>Option2</th>
      <th>Option3</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_questions.each do |add_question| %>
      <tr>
        <td><%= add_question.question %></td>
        <td><%= add_question.correctOption1 %></td>
        <td><%= add_question.Option2 %></td>
        <td><%= add_question.Option3 %></td>
        <td><%= link_to 'Show', add_question %></td>
        <td><%= link_to 'Edit', edit_add_question_path(add_question) %></td>
        <td><%= link_to 'Destroy', add_question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Question', new_add_question_path %>

</div>